// Placeholder brand name -- see CONTEXT.md §2: the name is deliberately
// undecided until Phase B, this is a stand-in so the UI has something to
// show, not a naming decision.
export const APP_NAME = "[Resto SaaS — nom à définir]";
